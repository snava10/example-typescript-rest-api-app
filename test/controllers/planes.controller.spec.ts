describe("Planes controller", () => {
  //   beforeAll(() => {});

  it("get all", () => {
    expect(false).toBeTruthy();
  });

  it("get by name", () => {
    expect(false).toBeTruthy();
  });

  it("create", () => {
    expect(false).toBeTruthy();
  });

  it("update", () => {
    expect(false).toBeTruthy();
  });
});
